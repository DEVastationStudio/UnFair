using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class mainScriptNoria : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        FadeController.FinishLoad();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
